[MODEL: Claude Code CLI]
What do you need? Fix compilation errors, complete the missing logic, review security posture, or all three?
The code has significant structural gaps:
1. **Struct**: `sanitizer` field declared, not initialized in `new()`
2. **`run()`**: `listener` never bound (missing `TcpListener::bind`)
3. **`sse_handler`**: `tx` spawned but never used — stream starves
4. **`message_handler`**: references `state` but no `State(state)` param
5. **`tools/call`**: `if let (params)` — malformed destructure, dead branch
6. **`handle_execute_plugin`**: `plugin` lookup missing, `target_real` undefined, loop `f findings` broken, `summary` build incomplete
7. **`JsonRpcRequest` / `CallToolResult`**: undefined structs
8. **Brace accounting**: multiple unclosed blocks — won't compile
Paste your target behavior or the full file and I'll fix it.